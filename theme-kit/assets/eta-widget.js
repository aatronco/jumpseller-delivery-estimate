/*! jumpseller-delivery-estimate v0.1.0 */
/* eta-comunas.js */
// Generated by scripts/build-comunas.mjs — do not edit by hand.
(function (root, factory) {
  if (typeof module === 'object' && module.exports) module.exports = factory();
  else { root.Eta = root.Eta || {}; root.Eta.Comunas = factory(); }
})(typeof self !== 'undefined' ? self : this, function () {
  'use strict';
  return [{"id":"8261162","name":"Aisén","region":"02"},{"id":"8261201","name":"Algarrobo","region":"01"},{"id":"8261425","name":"Alhué","region":"12"},{"id":"8261320","name":"Alto Biobío","region":"06"},{"id":"8261214","name":"Alto del Carmen","region":"05"},{"id":"8261202","name":"Alto Hospicio","region":"15"},{"id":"8261160","name":"Ancud","region":"14"},{"id":"8261217","name":"Andacollo","region":"07"},{"id":"8261199","name":"Angol","region":"04"},{"id":"8261467","name":"Antártica","region":"10"},{"id":"8261129","name":"Antofagasta","region":"03"},{"id":"8261322","name":"Antuco","region":"06"},{"id":"8261302","name":"Arauco","region":"06"},{"id":"8261387","name":"Arica","region":"16"},{"id":"8261309","name":"Buin","region":"12"},{"id":"8261304","name":"Bulnes","region":"18"},{"id":"8261236","name":"Cabildo","region":"01"},{"id":"8261179","name":"Cabo de Hornos","region":"10"},{"id":"8261305","name":"Cabrero","region":"06"},{"id":"8261495","name":"Calama","region":"03"},{"id":"8261175","name":"Calbuco","region":"14"},{"id":"8261223","name":"Caldera","region":"05"},{"id":"8261334","name":"Calera de Tango","region":"12"},{"id":"8261246","name":"Calle Larga","region":"01"},{"id":"8261252","name":"Camarones","region":"16"},{"id":"8261314","name":"Camiña","region":"15"},{"id":"8261137","name":"Canela","region":"07"},{"id":"8261315","name":"Cañete","region":"06"},{"id":"8261166","name":"Carahue","region":"04"},{"id":"8261450","name":"Cartagena","region":"01"},{"id":"8261254","name":"Casablanca","region":"01"},{"id":"8261426","name":"Castro","region":"14"},{"id":"8261229","name":"Catemu","region":"01"},{"id":"8261149","name":"Cauquenes","region":"11"},{"id":"8261369","name":"Cerrillos","region":"12"},{"id":"8261397","name":"Cerro Navia","region":"12"},{"id":"8261361","name":"Chaitén","region":"14"},{"id":"8261295","name":"Chanco","region":"11"},{"id":"8261221","name":"Chañaral","region":"05"},{"id":"8261274","name":"Chépica","region":"08"},{"id":"8261344","name":"Chiguayante","region":"06"},{"id":"8261446","name":"Chile Chico","region":"02"},{"id":"8261195","name":"Chillán","region":"18"},{"id":"8261339","name":"Chillán Viejo","region":"18"},{"id":"8261276","name":"Chimbarongo","region":"08"},{"id":"8261379","name":"Cholchol","region":"04"},{"id":"8261427","name":"Chonchi","region":"14"},{"id":"8261163","name":"Cisnes","region":"02"},{"id":"8261140","name":"Cobquecura","region":"18"},{"id":"8261177","name":"Cochamó","region":"14"},{"id":"8261363","name":"Cochrane","region":"02"},{"id":"8261269","name":"Codegua","region":"08"},{"id":"8261340","name":"Coelemu","region":"18"},{"id":"8261338","name":"Coihueco","region":"18"},{"id":"8261280","name":"Coinco","region":"08"},{"id":"8261145","name":"Colbún","region":"11"},{"id":"8261296","name":"Colchane","region":"15"},{"id":"8261384","name":"Colina","region":"12"},{"id":"8261169","name":"Collipulli","region":"04"},{"id":"8261258","name":"Coltauco","region":"08"},{"id":"8261230","name":"Combarbalá","region":"07"},{"id":"6693568","name":"Concepción","region":"06"},{"id":"8261398","name":"Conchalí","region":"12"},{"id":"8261183","name":"Concón","region":"01"},{"id":"8261313","name":"Constitución","region":"11"},{"id":"8261331","name":"Contulmo","region":"06"},{"id":"8261212","name":"Copiapó","region":"05"},{"id":"8261227","name":"Coquimbo","region":"07"},{"id":"8261347","name":"Coronel","region":"06"},{"id":"8261460","name":"Corral","region":"17"},{"id":"8261198","name":"Coyhaique","region":"02"},{"id":"8261376","name":"Cunco","region":"04"},{"id":"8261386","name":"Curacautín","region":"04"},{"id":"8261456","name":"Curacaví","region":"12"},{"id":"8261430","name":"Curaco de Vélez","region":"14"},{"id":"8261152","name":"Curanilahue","region":"06"},{"id":"8261173","name":"Curarrehue","region":"04"},{"id":"8261308","name":"Curepto","region":"11"},{"id":"8261139","name":"Curicó","region":"11"},{"id":"8261159","name":"Dalcahue","region":"14"},{"id":"8261132","name":"Diego de Almagro","region":"05"},{"id":"8261283","name":"Doñihue","region":"08"},{"id":"8261399","name":"El Bosque","region":"12"},{"id":"8261328","name":"El Carmen","region":"18"},{"id":"8261346","name":"El Monte","region":"12"},{"id":"8261225","name":"El Quisco","region":"01"},{"id":"7798677","name":"El Tabo","region":"01"},{"id":"8261298","name":"Empedrado","region":"11"},{"id":"8261357","name":"Ercilla","region":"04"},{"id":"8261400","name":"Estación Central","region":"12"},{"id":"8261342","name":"Florida","region":"06"},{"id":"8261381","name":"Freire","region":"04"},{"id":"8261215","name":"Freirina","region":"05"},{"id":"8261406","name":"Fresia","region":"14"},{"id":"8261382","name":"Frutillar","region":"14"},{"id":"8261437","name":"Futaleufú","region":"14"},{"id":"8261366","name":"Futrono","region":"17"},{"id":"8261378","name":"Galvarino","region":"04"},{"id":"8261451","name":"General Lagos","region":"16"},{"id":"8261403","name":"Gorbea","region":"04"},{"id":"8261271","name":"Graneros","region":"08"},{"id":"8261164","name":"Guaitecas","region":"02"},{"id":"8261442","name":"Hijuelas","region":"01"},{"id":"8261375","name":"Hualaihué","region":"14"},{"id":"8261333","name":"Hualañé","region":"11"},{"id":"3888526","name":"Hualpén","region":"06"},{"id":"8261345","name":"Hualqui","region":"06"},{"id":"8261206","name":"Huara","region":"15"},{"id":"8261131","name":"Huasco","region":"05"},{"id":"8261432","name":"Huechuraba","region":"12"},{"id":"8261136","name":"Illapel","region":"07"},{"id":"8261421","name":"Independencia","region":"12"},{"id":"8261207","name":"Iquique","region":"15"},{"id":"8261364","name":"Isla de Maipo","region":"12"},{"id":"8261466","name":"Isla de Pascua","region":"01"},{"id":"8261465","name":"Juan Fernández","region":"01"},{"id":"8261219","name":"La Calera","region":"01"},{"id":"8261372","name":"La Cisterna","region":"12"},{"id":"8261222","name":"La Cruz","region":"01"},{"id":"8261257","name":"La Estrella","region":"08"},{"id":"8261405","name":"La Florida","region":"12"},{"id":"8261316","name":"La Granja","region":"12"},{"id":"8261133","name":"La Higuera","region":"07"},{"id":"8261234","name":"La Ligua","region":"01"},{"id":"8261407","name":"La Pintana","region":"12"},{"id":"8261410","name":"La Reina","region":"12"},{"id":"8261226","name":"La Serena","region":"07"},{"id":"8261355","name":"La Unión","region":"17"},{"id":"8261367","name":"Lago Ranco","region":"17"},{"id":"8261161","name":"Lago Verde","region":"02"},{"id":"8261394","name":"Laguna Blanca","region":"10"},{"id":"8261353","name":"Laja","region":"06"},{"id":"8261454","name":"Lampa","region":"12"},{"id":"8261462","name":"Lanco","region":"17"},{"id":"8261260","name":"Las Cabras","region":"08"},{"id":"8261428","name":"Las Condes","region":"12"},{"id":"8261368","name":"Lautaro","region":"04"},{"id":"8261150","name":"Lebu","region":"06"},{"id":"8261286","name":"Licantén","region":"11"},{"id":"8261200","name":"Limache","region":"01"},{"id":"8261146","name":"Linares","region":"11"},{"id":"8261255","name":"Litueche","region":"08"},{"id":"8261374","name":"Llanquihue","region":"14"},{"id":"8261237","name":"Llay Llay","region":"01"},{"id":"8261390","name":"Lo Barnechea","region":"12"},{"id":"8261156","name":"Lo Espejo","region":"12"},{"id":"8261174","name":"Lo Prado","region":"12"},{"id":"8261272","name":"Lolol","region":"08"},{"id":"8261401","name":"Loncoche","region":"04"},{"id":"8261147","name":"Longaví","region":"11"},{"id":"8261172","name":"Lonquimay","region":"04"},{"id":"8261301","name":"Los Álamos","region":"06"},{"id":"8261245","name":"Los Andes","region":"01"},{"id":"8261144","name":"Los Ángeles","region":"06"},{"id":"8261445","name":"Los Lagos","region":"17"},{"id":"8261420","name":"Los Muermos","region":"14"},{"id":"8261359","name":"Los Sauces","region":"04"},{"id":"8261193","name":"Los Vilos","region":"07"},{"id":"8261348","name":"Lota","region":"06"},{"id":"8261170","name":"Lumaco","region":"04"},{"id":"8261191","name":"Machalí","region":"08"},{"id":"8261194","name":"Macul","region":"12"},{"id":"8261458","name":"Máfil","region":"17"},{"id":"8261434","name":"Maipú","region":"12"},{"id":"8261277","name":"Malloa","region":"08"},{"id":"8261259","name":"Marchihue","region":"08"},{"id":"8261208","name":"María Elena","region":"03"},{"id":"8261439","name":"María Pinto","region":"12"},{"id":"8261365","name":"Mariquina","region":"17"},{"id":"8261300","name":"Maule","region":"11"},{"id":"8261415","name":"Maullín","region":"14"},{"id":"8261203","name":"Mejillones","region":"03"},{"id":"8261404","name":"Melipeuco","region":"04"},{"id":"8261185","name":"Melipilla","region":"12"},{"id":"8261189","name":"Molina","region":"11"},{"id":"8261135","name":"Monte Patria","region":"07"},{"id":"8261284","name":"Mostazal","region":"08"},{"id":"8261318","name":"Mulchén","region":"06"},{"id":"8261153","name":"Nacimiento","region":"06"},{"id":"8261266","name":"Nancagua","region":"08"},{"id":"8261192","name":"Natales","region":"10"},{"id":"8261253","name":"Navidad","region":"08"},{"id":"8261343","name":"Negrete","region":"06"},{"id":"8261142","name":"Ninhue","region":"18"},{"id":"8261218","name":"Nogales","region":"01"},{"id":"8261358","name":"Nueva Imperial","region":"04"},{"id":"8261336","name":"Ñiquén","region":"18"},{"id":"8261178","name":"Ñuñoa","region":"12"},{"id":"8261464","name":"O’Higgins","region":"02"},{"id":"8261282","name":"Olivar","region":"08"},{"id":"8261205","name":"Ollagüe","region":"03"},{"id":"8261449","name":"Olmué","region":"01"},{"id":"8261411","name":"Osorno","region":"14"},{"id":"8261224","name":"Ovalle","region":"07"},{"id":"8261231","name":"Padre Hurtado","region":"12"},{"id":"8261351","name":"Padre las Casas","region":"04"},{"id":"8261228","name":"Paiguano","region":"07"},{"id":"8261459","name":"Paillaco","region":"17"},{"id":"8261424","name":"Paine","region":"12"},{"id":"8261360","name":"Palena","region":"14"},{"id":"8261264","name":"Palmilla","region":"08"},{"id":"8261354","name":"Panguipulli","region":"17"},{"id":"8261238","name":"Panquehue","region":"01"},{"id":"8261391","name":"Papudo","region":"01"},{"id":"8261270","name":"Paredones","region":"08"},{"id":"8261148","name":"Parral","region":"11"},{"id":"8261157","name":"Pedro Aguirre Cerda","region":"12"},{"id":"8261290","name":"Pelarco","region":"11"},{"id":"8261306","name":"Pelluhue","region":"11"},{"id":"8261327","name":"Pemuco","region":"18"},{"id":"8261297","name":"Pencahue","region":"11"},{"id":"8261303","name":"Penco","region":"06"},{"id":"8261457","name":"Peñaflor","region":"12"},{"id":"8261413","name":"Peñalolén","region":"12"},{"id":"8261267","name":"Peralillo","region":"08"},{"id":"8261307","name":"Perquenco","region":"04"},{"id":"8261235","name":"Petorca","region":"01"},{"id":"8261186","name":"Peumo","region":"08"},{"id":"8261128","name":"Pica","region":"15"},{"id":"8261262","name":"Pichidegua","region":"08"},{"id":"8261256","name":"Pichilemu","region":"08"},{"id":"8261323","name":"Pinto","region":"18"},{"id":"8261440","name":"Pirque","region":"12"},{"id":"8261373","name":"Pitrufquén","region":"04"},{"id":"8261261","name":"Placilla","region":"08"},{"id":"8261312","name":"Portezuelo","region":"18"},{"id":"8261448","name":"Porvenir","region":"10"},{"id":"8261350","name":"Pozo Almonte","region":"15"},{"id":"8261447","name":"Primavera","region":"10"},{"id":"8261414","name":"Providencia","region":"12"},{"id":"8261248","name":"Puchuncaví","region":"01"},{"id":"8261402","name":"Pucón","region":"04"},{"id":"8261436","name":"Pudahuel","region":"12"},{"id":"8261423","name":"Puente Alto","region":"12"},{"id":"8261383","name":"Puerto Montt","region":"14"},{"id":"8261412","name":"Puerto Octay","region":"14"},{"id":"8261155","name":"Puerto Varas","region":"14"},{"id":"8261268","name":"Pumanque","region":"08"},{"id":"8261232","name":"Punitaqui","region":"07"},{"id":"8261182","name":"Punta Arenas","region":"10"},{"id":"8261167","name":"Puqueldón","region":"14"},{"id":"8261392","name":"Purén","region":"04"},{"id":"8261408","name":"Purranque","region":"14"},{"id":"8261240","name":"Putaendo","region":"01"},{"id":"8261463","name":"Putre","region":"16"},{"id":"8261443","name":"Puyehue","region":"14"},{"id":"8261429","name":"Queilén","region":"14"},{"id":"8261176","name":"Quellón","region":"14"},{"id":"8261154","name":"Quemchi","region":"14"},{"id":"8261319","name":"Quilaco","region":"06"},{"id":"8261431","name":"Quilicura","region":"12"},{"id":"8261324","name":"Quilleco","region":"06"},{"id":"8261171","name":"Quillón","region":"18"},{"id":"8261275","name":"Quillota","region":"01"},{"id":"8261204","name":"Quilpué","region":"01"},{"id":"8261422","name":"Quinchao","region":"14"},{"id":"8261273","name":"Quinta de Tilcoco","region":"08"},{"id":"8261416","name":"Quinta Normal","region":"12"},{"id":"8261233","name":"Quintero","region":"01"},{"id":"8261141","name":"Quirihue","region":"18"},{"id":"8261278","name":"Rancagua","region":"08"},{"id":"8261341","name":"Ránquil","region":"18"},{"id":"8261289","name":"Rauco","region":"11"},{"id":"8261417","name":"Recoleta","region":"12"},{"id":"8261388","name":"Renaico","region":"04"},{"id":"8261418","name":"Renca","region":"12"},{"id":"8261279","name":"Rengo","region":"08"},{"id":"8261281","name":"Requínoa","region":"08"},{"id":"8261299","name":"Retiro","region":"11"},{"id":"8261389","name":"Rinconada","region":"01"},{"id":"8261356","name":"Río Bueno","region":"17"},{"id":"8261294","name":"Río Claro","region":"11"},{"id":"7874689","name":"Río Hurtado","region":"07"},{"id":"8261444","name":"Río Ibáñez","region":"02"},{"id":"8261409","name":"Río Negro","region":"14"},{"id":"8261180","name":"Río Verde","region":"10"},{"id":"8261288","name":"Romeral","region":"11"},{"id":"8261241","name":"Saavedra","region":"04"},{"id":"8261287","name":"Sagrada Familia","region":"11"},{"id":"8261138","name":"Salamanca","region":"07"},{"id":"8261250","name":"San Antonio","region":"01"},{"id":"8261438","name":"San Bernardo","region":"12"},{"id":"8261190","name":"San Carlos","region":"18"},{"id":"8261188","name":"San Clemente","region":"11"},{"id":"8261243","name":"San Esteban","region":"01"},{"id":"8261337","name":"San Fabián","region":"18"},{"id":"8261220","name":"San Felipe","region":"01"},{"id":"8261187","name":"San Fernando","region":"08"},{"id":"8261452","name":"San Gregorio","region":"10"},{"id":"8261329","name":"San Ignacio","region":"18"},{"id":"8261310","name":"San Javier","region":"11"},{"id":"8261419","name":"San Joaquín","region":"12"},{"id":"8261184","name":"San José de Maipo","region":"12"},{"id":"8261433","name":"San Juan de la Costa","region":"14"},{"id":"8261377","name":"San Miguel","region":"12"},{"id":"8261335","name":"San Nicolás","region":"18"},{"id":"8261435","name":"San Pablo","region":"14"},{"id":"8261455","name":"San Pedro","region":"12"},{"id":"8261210","name":"San Pedro de Atacama","region":"03"},{"id":"6693571","name":"San Pedro de la Paz","region":"06"},{"id":"8261293","name":"San Rafael","region":"11"},{"id":"8261158","name":"San Ramón","region":"12"},{"id":"8261349","name":"San Rosendo","region":"06"},{"id":"8261263","name":"San Vicente","region":"08"},{"id":"8261321","name":"Santa Bárbara","region":"06"},{"id":"8261265","name":"Santa Cruz","region":"08"},{"id":"8261196","name":"Santa Juana","region":"06"},{"id":"8261244","name":"Santa María","region":"01"},{"id":"8261395","name":"Santiago","region":"12"},{"id":"8261251","name":"Santo Domingo","region":"01"},{"id":"8261211","name":"Sierra Gorda","region":"03"},{"id":"8261332","name":"Talagante","region":"12"},{"id":"8261285","name":"Talca","region":"11"},{"id":"6693599","name":"Talcahuano","region":"06"},{"id":"8261130","name":"Taltal","region":"03"},{"id":"8261311","name":"Temuco","region":"04"},{"id":"8261291","name":"Teno","region":"11"},{"id":"8261371","name":"Teodoro Schmidt","region":"04"},{"id":"8261213","name":"Tierra Amarilla","region":"05"},{"id":"8261453","name":"Tiltil","region":"12"},{"id":"8261181","name":"Timaukel","region":"10"},{"id":"8261330","name":"Tirúa","region":"06"},{"id":"8261216","name":"Tocopilla","region":"03"},{"id":"8261393","name":"Toltén","region":"04"},{"id":"8261151","name":"Tomé","region":"06"},{"id":"3869640","name":"Tongoy","region":"07"},{"id":"8261441","name":"Torres del Paine","region":"10"},{"id":"8261362","name":"Tortel","region":"02"},{"id":"8261370","name":"Traiguén","region":"04"},{"id":"8261143","name":"Treguaco","region":"18"},{"id":"8261325","name":"Tucapel","region":"06"},{"id":"8261461","name":"Valdivia","region":"17"},{"id":"8261168","name":"Vallenar","region":"05"},{"id":"8261249","name":"Valparaíso","region":"01"},{"id":"8261317","name":"Vichuquén","region":"11"},{"id":"8261352","name":"Victoria","region":"04"},{"id":"8261134","name":"Vicuña","region":"07"},{"id":"8261385","name":"Vilcún","region":"04"},{"id":"8261292","name":"Villa Alegre","region":"11"},{"id":"8261165","name":"Villa Alemana","region":"01"},{"id":"8261396","name":"Villarrica","region":"04"},{"id":"8261239","name":"Viña del Mar","region":"01"},{"id":"8261380","name":"Vitacura","region":"12"},{"id":"8261242","name":"Yerbas Buenas","region":"11"},{"id":"8261197","name":"Yumbel","region":"06"},{"id":"8261326","name":"Yungay","region":"18"},{"id":"8261247","name":"Zapallar","region":"01"}];
});

/* eta-rules.js */
(function (root, factory) {
  if (typeof module === 'object' && module.exports) module.exports = factory();
  else { root.Eta = root.Eta || {}; root.Eta.Rules = factory(); }
})(typeof self !== 'undefined' ? self : this, function () {
  'use strict';

  var noop = function () {};

  function normalizeComuna(name) {
    return String(name || '')
      .normalize('NFD').replace(/[̀-ͯ]/g, '')
      .toLowerCase().replace(/\s+/g, ' ').trim();
  }

  function parseList(text) {
    return String(text || '').split(/[\n,;]+/).map(function (s) { return s.trim(); }).filter(Boolean);
  }

  function parseTable(text, warn) {
    warn = warn || noop;
    var tiers = [];
    parseList(text).forEach(function (line) {
      var m = line.match(/^(\*|\d+(?:\.\d+)?)\s*=\s*([\d.]+)$/);
      if (!m) { warn('[eta] invalid flat-rate line: ' + line); return; }
      tiers.push({
        maxKg: m[1] === '*' ? Infinity : parseFloat(m[1]),
        price: parseInt(m[2].replace(/\./g, ''), 10),
      });
    });
    return tiers.sort(function (a, b) { return a.maxKg - b.maxKg; });
  }

  function resolveComunas(names, comunas, warn) {
    warn = warn || noop;
    var index = {};
    comunas.forEach(function (c) { index[normalizeComuna(c.name)] = c; });
    var out = [];
    names.forEach(function (name) {
      var c = index[normalizeComuna(name)];
      if (c) out.push(c); else warn('[eta] unknown comuna: ' + name);
    });
    return out;
  }

  function flatRate(comunaId, kg, cfg) {
    if (cfg.flatComunaIds.indexOf(String(comunaId)) === -1) return null;
    for (var i = 0; i < cfg.flatTiers.length; i++) {
      if (kg <= cfg.flatTiers[i].maxKg) return cfg.flatTiers[i].price;
    }
    return null;
  }

  function parseClp(text) {
    var digits = String(text == null ? '' : text).replace(/[^\d]/g, '');
    return digits ? parseInt(digits, 10) : null;
  }

  function formatClp(n) {
    return '$' + String(Math.round(n)).replace(/\B(?=(\d{3})+(?!\d))/g, '.');
  }

  function parseServices(text) {
    return parseList(text).map(function (s) {
      var parts = s.split(':');
      return parts[0].trim().toLowerCase() + ':' + (parts[1] || '').trim().toUpperCase();
    });
  }

  function filterHomeDelivery(rates, services) {
    if (!Array.isArray(rates)) return [];
    var out = [];
    rates.forEach(function (r) {
      var key = String(r.fulfillment_company).toLowerCase() + ':' + String(r.service_code).toUpperCase();
      var amount = parseClp(r.amount);
      if (services.indexOf(key) !== -1 && amount > 0) {
        out.push({ company: r.fulfillment_company, label: r.service_label, amount: amount });
      }
    });
    return out;
  }

  function pickPrice(flat, quotes) {
    var best = null;
    (quotes || []).forEach(function (q) { if (best === null || q.amount < best) best = q.amount; });
    if (flat != null && (best === null || flat <= best)) return { price: flat, source: 'flat' };
    if (best !== null) return { price: best, source: 'courier' };
    return { price: null, source: null };
  }

  function cartWeight(cart, productId, variantId) {
    var lines = (cart && Array.isArray(cart.products)) ? cart.products : [];
    var kg = 0;
    var containsCurrent = false;
    lines.forEach(function (l) {
      kg += (Number(l.weight) || 0) * (Number(l.qty) || 0);
      if (String(l.product_id) === String(productId) &&
          String(l.variant_id == null ? '' : l.variant_id) === String(variantId == null ? '' : variantId)) {
        containsCurrent = true;
      }
    });
    return { kg: Math.round(kg * 1000) / 1000, containsCurrent: containsCurrent };
  }

  function variantWeight(product, variantId) {
    if (!product) return 0;
    var variants = product.variants || [];
    for (var i = 0; i < variants.length; i++) {
      if (String(variants[i].id) === String(variantId) && Number(variants[i].weight) > 0) {
        return Number(variants[i].weight);
      }
    }
    return Number(product.weight) || 0;
  }

  function roundKg(kg) {
    return Math.max(0.5, Math.ceil(kg * 2) / 2);
  }

  var WEEKDAYS_ES = ['domingo', 'lunes', 'martes', 'miércoles', 'jueves', 'viernes', 'sábado'];
  var WEEKDAY_INDEX = { Sun: 0, Mon: 1, Tue: 2, Wed: 3, Thu: 4, Fri: 5, Sat: 6 };

  function parseCutoff(text, warn) {
    var m = String(text || '').trim().match(/^(\d{1,2}):(\d{2})$/);
    if (!m || +m[1] > 23 || +m[2] > 59) { (warn || noop)('[eta] invalid cutoff: ' + text); return 840; }
    return (+m[1]) * 60 + (+m[2]);
  }

  function localParts(now, timeZone) {
    var parts = {};
    new Intl.DateTimeFormat('en-US', {
      timeZone: timeZone, year: 'numeric', month: '2-digit', day: '2-digit',
      hour: '2-digit', minute: '2-digit', weekday: 'short', hourCycle: 'h23',
    }).formatToParts(now).forEach(function (p) { parts[p.type] = p.value; });
    return {
      date: parts.year + '-' + parts.month + '-' + parts.day,
      weekday: WEEKDAY_INDEX[parts.weekday],
      minutes: (+parts.hour) * 60 + (+parts.minute),
    };
  }

  function addDays(dateStr, n) {
    var d = new Date(dateStr + 'T12:00:00Z');
    d.setUTCDate(d.getUTCDate() + n);
    return { date: d.toISOString().slice(0, 10), weekday: d.getUTCDay() };
  }

  function promiseDay(now, cfg) {
    var local = localParts(now, cfg.timeZone || 'America/Santiago');
    var isDispatch = function (date, weekday) {
      return cfg.dispatchDays.indexOf(weekday) !== -1 && cfg.holidays.indexOf(date) === -1;
    };
    if (isDispatch(local.date, local.weekday) && local.minutes < cfg.cutoffMin) {
      return { kind: 'today', label: 'hoy', minutesLeft: cfg.cutoffMin - local.minutes };
    }
    for (var n = 1; n <= 14; n++) {
      var day = addDays(local.date, n);
      if (isDispatch(day.date, day.weekday)) {
        return n === 1
          ? { kind: 'tomorrow', label: 'mañana', minutesLeft: null }
          : { kind: 'later', label: 'el ' + WEEKDAYS_ES[day.weekday], minutesLeft: null };
      }
    }
    return null;
  }

  function formatCountdown(minutes) {
    var h = Math.floor(minutes / 60);
    var m = minutes % 60;
    if (h && m) return h + ' h ' + m + ' min';
    return h ? h + ' h' : m + ' min';
  }

  return {
    normalizeComuna: normalizeComuna, parseList: parseList, parseTable: parseTable,
    resolveComunas: resolveComunas, flatRate: flatRate, parseClp: parseClp, formatClp: formatClp,
    parseServices: parseServices, filterHomeDelivery: filterHomeDelivery, pickPrice: pickPrice,
    cartWeight: cartWeight, variantWeight: variantWeight, roundKg: roundKg,
    parseCutoff: parseCutoff, promiseDay: promiseDay, formatCountdown: formatCountdown,
  };
});

/* eta-store.js */
(function (root, factory) {
  if (typeof module === 'object' && module.exports) module.exports = factory();
  else { root.Eta = root.Eta || {}; root.Eta.Store = factory(); }
})(typeof self !== 'undefined' ? self : this, function () {
  'use strict';

  var KEY = 'eta:comuna';

  function createStore(opts) {
    var storage = opts.storage || null;
    var target = opts.eventTarget || null;
    var memory = null;

    function get() {
      try {
        var raw = storage && storage.getItem(KEY);
        if (raw) return JSON.parse(raw);
      } catch (e) { /* blocked or corrupted: fall through */ }
      return memory;
    }

    function set(comuna) {
      memory = comuna;
      try { if (storage) storage.setItem(KEY, JSON.stringify(comuna)); } catch (e) { /* memory only */ }
      if (target) {
        var Ctor = (target.defaultView && target.defaultView.CustomEvent) ||
          (typeof CustomEvent !== 'undefined' ? CustomEvent : null);
        if (Ctor) target.dispatchEvent(new Ctor('eta:comuna-changed', { detail: comuna }));
      }
    }

    return { get: get, set: set };
  }

  return { createStore: createStore, KEY: KEY };
});

/* eta-data.js */
(function (root, factory) {
  if (typeof module === 'object' && module.exports) module.exports = factory();
  else { root.Eta = root.Eta || {}; root.Eta.Data = factory(); }
})(typeof self !== 'undefined' ? self : this, function () {
  'use strict';

  var QUOTE_URL = 'https://api.jumpseller.com/landing/estimate_rates.json';

  function createData(opts) {
    var rules = opts.rules;
    var timeoutMs = opts.timeoutMs || 10000;
    var ttlMs = opts.ttlMs || 6 * 3600e3;
    var emptyTtlMs = opts.emptyTtlMs || 10 * 60e3;
    var productCache = {};

    function readCache(key) {
      try { var raw = opts.storage && opts.storage.getItem(key); return raw ? JSON.parse(raw) : null; }
      catch (e) { return null; }
    }
    function writeCache(key, value) {
      try { if (opts.storage) opts.storage.setItem(key, JSON.stringify(value)); } catch (e) { /* ignore */ }
    }

    function loadProduct(productId) {
      if (!productCache[productId]) {
        productCache[productId] = opts.fetch('/api/products/' + productId + '.json')
          .then(function (r) { return r.ok ? r.json() : null; })
          .catch(function () { return null; });
      }
      return productCache[productId];
    }

    function weightFor(productId, variantId) {
      var cartP = Promise.resolve().then(opts.getCart).catch(function () { return null; });
      return Promise.all([cartP, loadProduct(productId)]).then(function (res) {
        var cart = rules.cartWeight(res[0], productId, variantId);
        var item = rules.variantWeight(res[1], variantId);
        return Math.round((cart.kg + (cart.containsCurrent ? 0 : item)) * 1000) / 1000;
      });
    }

    function quote(comuna, kg) {
      var weight = rules.roundKg(kg);
      var box = opts.box.length + 'x' + opts.box.width + 'x' + opts.box.height;
      var key = 'eta:q:' + opts.origin.id + ':' + box + ':' + opts.services.join(',') + '|' + comuna.id + '|' + weight;
      var cached = readCache(key);
      if (cached && Array.isArray(cached.quotes)) {
        var ttl = cached.quotes.length ? ttlMs : emptyTtlMs;
        if (opts.now() - cached.t < ttl) return Promise.resolve(cached.quotes);
      }
      var controller = new AbortController();
      var timer = setTimeout(function () { controller.abort(); }, timeoutMs);
      var body = { rates: {
        origin_address: { country: 'CL', region: opts.origin.region, municipality: opts.origin.id },
        shipping_address: { country: 'CL', region: comuna.region, municipality: comuna.id },
        packages_dimensions: [{ length: opts.box.length, width: opts.box.width, height: opts.box.height, weight: weight }],
      } };
      return opts.fetch(QUOTE_URL, {
        method: 'POST', headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(body), signal: controller.signal,
      }).then(function (r) {
        if (!r.ok) throw new Error('HTTP ' + r.status);
        return r.json().then(function (rates) {
          var quotes = rules.filterHomeDelivery(rates, opts.services);
          writeCache(key, { t: opts.now(), quotes: quotes });
          return quotes;
        });
      }).catch(function () {
        // An error behaves like [] for later calls (short TTL); this call still resolves null.
        writeCache(key, { t: opts.now(), quotes: [] });
        return null;
      }).finally(function () { clearTimeout(timer); });
    }

    return { weightFor: weightFor, quote: quote };
  }

  return { createData: createData, QUOTE_URL: QUOTE_URL };
});

/* eta-ui.js */
(function (root, factory) {
  if (typeof module === 'object' && module.exports) module.exports = factory();
  else { root.Eta = root.Eta || {}; root.Eta.UI = factory(); }
})(typeof self !== 'undefined' ? self : this, function () {
  'use strict';

  var GEO_FIELDS = ['municipality', 'city_district', 'town', 'village', 'suburb', 'city', 'county'];

  // Shoppers type "Aysén"/"Aysen" and Nominatim may return "Aysén", but the
  // official comuna name in the data is "Aisén". Map the normalized alias to
  // the normalized official name so both search and geolocation match it.
  var COMUNA_ALIASES = { aysen: 'aisen' };

  function normalize(name, rules) {
    var n = rules.normalizeComuna(name);
    return COMUNA_ALIASES[n] || n;
  }

  function escapeHtml(s) {
    return String(s).replace(/[&<>"']/g, function (c) {
      return { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c];
    });
  }

  var changeBtn = function (text) {
    return '<button type="button" class="eta-widget__change" data-eta-open>' + text + '</button>';
  };

  function renderWidget(el, state, rules) {
    var html;
    var name = state.comuna ? escapeHtml(state.comuna.name) : '';
    if (state.kind === 'no-comuna') {
      html = '<p class="eta-widget__title">Envío a todo Chile</p>' +
        '<p class="eta-widget__meta">' + changeBtn('📍 ¿Dónde lo recibes?') + '</p>';
    } else if (state.kind === 'loading') {
      html = '<p class="eta-widget__title">Envío a ' + name + ' <span class="eta-widget__skeleton" aria-hidden="true"></span></p>' +
        '<p class="eta-widget__meta">&nbsp;</p>';
    } else if (state.kind === 'unavailable') {
      html = '<p class="eta-widget__title">Envío a ' + name + ': se calcula en el checkout</p>' +
        '<p class="eta-widget__meta">' + changeBtn('Cambiar comuna') + '</p>';
    } else {
      var price = '<strong>' + rules.formatClp(state.price) + '</strong>';
      var p = state.promise;
      if (state.source === 'flat' && p) {
        html = '<p class="eta-widget__title">Llega <strong>' + p.label + '</strong> por ' + price + '</p>' +
          '<p class="eta-widget__meta">a ' + name + ' · ' + changeBtn('Cambiar') + '</p>';
        if (state.showCountdown && p.kind === 'today' && p.minutesLeft > 0) {
          html += '<p class="eta-widget__countdown">Compra en las próximas ' + rules.formatCountdown(p.minutesLeft) + '</p>';
        }
      } else {
        html = '<p class="eta-widget__title">Envío a ' + name + ' desde ' + price + '</p>' +
          '<p class="eta-widget__meta">' + changeBtn('Cambiar comuna') + '</p>';
      }
    }
    var className = 'eta-widget eta-widget--' + state.kind;
    html = '<span class="eta-widget__icon" aria-hidden="true">🚚</span><div class="eta-widget__body">' + html + '</div>';
    // Skip identical writes so aria-live regions are not re-announced. The source
    // HTML is kept on the element because innerHTML re-serializes (e.g. data-eta-open="").
    if (el.className === className && el._etaHtml === html) return;
    el.className = className;
    el.innerHTML = html;
    el._etaHtml = html;
  }

  function searchComunas(query, comunas, rules, limit) {
    limit = limit || 8;
    var q = normalize(query, rules);
    if (!q) return [];
    var prefix = [];
    var inner = [];
    comunas.forEach(function (c) {
      var n = normalize(c.name, rules);
      if (n.indexOf(q) === 0) prefix.push(c);
      else if (n.indexOf(q) > 0) inner.push(c);
    });
    return prefix.concat(inner).slice(0, limit);
  }

  function geoToComuna(result, comunas, rules) {
    if (!result) return null;
    var index = {};
    comunas.forEach(function (c) { index[normalize(c.name, rules)] = c; });
    var address = result.address || {};
    var candidates = [result.name].concat(GEO_FIELDS.map(function (f) { return address[f]; }));
    for (var i = 0; i < candidates.length; i++) {
      var hit = candidates[i] && index[normalize(candidates[i], rules)];
      if (hit) return hit;
    }
    return null;
  }

  function renderLabel(el, comuna) {
    el.textContent = comuna ? 'Enviar a ' + comuna.name : '¿Dónde lo recibes?';
  }

  function mountPanel(panel, opts) {
    var input = panel.querySelector('.eta-panel__search');
    var list = panel.querySelector('.eta-panel__list');
    var msg = panel.querySelector('[data-eta-msg]');
    var geoBtn = panel.querySelector('[data-eta-geo]');

    var opener = null;

    function showMsg(text) { msg.textContent = text; msg.hidden = !text; }
    function close() {
      if (panel.hidden) return;
      panel.hidden = true;
      if (opener && opener.isConnected && typeof opener.focus === 'function') opener.focus();
      opener = null;
    }
    function open() {
      opener = panel.ownerDocument.activeElement;
      panel.hidden = false; input.value = ''; list.innerHTML = ''; showMsg('');
      if (input.focus) input.focus();
    }
    function pick(c) { opts.onPick(c); close(); }

    input.addEventListener('input', function () {
      var matches = searchComunas(input.value, opts.comunas, opts.rules);
      list.innerHTML = '';
      matches.forEach(function (c) {
        var li = panel.ownerDocument.createElement('li');
        var b = panel.ownerDocument.createElement('button');
        b.type = 'button'; b.textContent = c.name;
        b.addEventListener('click', function () { pick(c); });
        li.appendChild(b); list.appendChild(li);
      });
      showMsg(input.value.trim() && !matches.length ? 'No encontramos esa comuna' : '');
    });
    geoBtn.addEventListener('click', function () {
      showMsg('Buscando tu ubicación…');
      opts.geolocate().then(function (c) {
        if (c) pick(c); else showMsg('No pudimos detectar tu comuna. Búscala en la lista.');
      }, function () { showMsg('No pudimos detectar tu comuna. Búscala en la lista.'); });
    });
    panel.querySelectorAll('[data-eta-close]').forEach(function (b) { b.addEventListener('click', close); });
    panel.ownerDocument.addEventListener('keydown', function (e) { if (e.key === 'Escape') close(); });

    return { open: open, close: close };
  }

  return {
    renderWidget: renderWidget, searchComunas: searchComunas, geoToComuna: geoToComuna,
    renderLabel: renderLabel, mountPanel: mountPanel, escapeHtml: escapeHtml,
  };
});

/* eta-boot.js */
(function (root, factory) {
  if (typeof module === 'object' && module.exports) module.exports = factory();
  else { root.Eta = root.Eta || {}; root.Eta.Boot = factory(); }
})(typeof self !== 'undefined' ? self : this, function () {
  'use strict';

  var DEFAULT_BOX = { length: 30, width: 20, height: 10 };

  function readConfig(ds, comunas, rules, warn) {
    warn = warn || function (m) { if (typeof console !== 'undefined') console.warn(m); };
    var origin = rules.resolveComunas([ds.origin || 'Santiago'], comunas, warn)[0] ||
      rules.resolveComunas(['Santiago'], comunas)[0];
    var dims = String(ds.box || '').split('x').map(Number);
    var box = dims.length === 3 && dims.every(function (n) { return n > 0; })
      ? { length: dims[0], width: dims[1], height: dims[2] } : DEFAULT_BOX;
    return {
      origin: origin,
      cutoffMin: rules.parseCutoff(ds.cutoff || '14:00', warn),
      dispatchDays: rules.parseList(ds.days).map(Number).filter(function (n) { return n >= 0 && n <= 6; }),
      holidays: rules.parseList(ds.holidays),
      flatComunaIds: rules.resolveComunas(rules.parseList(ds.flatComunas), comunas, warn).map(function (c) { return c.id; }),
      flatTiers: rules.parseTable(ds.flatTable, warn),
      showCountdown: ds.countdown === '1',
      box: box,
      services: rules.parseServices(ds.services),
    };
  }

  function start(o) {
    var doc = o.doc, cfg = o.config, rules = o.rules, ui = o.ui;
    var widget = doc.querySelector('[data-eta-widget]');
    var labels = doc.querySelectorAll('[data-eta-label]');
    var panelEl = doc.querySelector('[data-eta-panel]');
    var seq = 0;
    var last = null; // { comuna, kg, flat, quotes } from the latest completed update

    var panel = panelEl && ui.mountPanel(panelEl, {
      comunas: o.comunas, rules: rules,
      onPick: function (c) { o.store.set(c); },
      geolocate: o.geolocate || function () { return Promise.resolve(null); },
    });
    doc.addEventListener('click', function (e) {
      var t = e.target && e.target.closest && e.target.closest('[data-eta-open]');
      if (t && panel) { e.preventDefault(); panel.open(); }
    });

    function renderPriced(comuna, flat, quotes) {
      var pick = rules.pickPrice(flat, quotes);
      if (pick.price == null) { ui.renderWidget(widget, { kind: 'unavailable', comuna: comuna }, rules); return; }
      ui.renderWidget(widget, { kind: 'priced', comuna: comuna, price: pick.price, source: pick.source,
        promise: pick.source === 'flat' ? rules.promiseDay(o.now(), cfg) : null, showCountdown: cfg.showCountdown }, rules);
    }

    function update() {
      var mySeq = ++seq;
      last = null;
      var comuna = o.store.get();
      labels.forEach(function (l) { ui.renderLabel(l, comuna); });
      if (!widget) return Promise.resolve();
      if (!comuna) { ui.renderWidget(widget, { kind: 'no-comuna' }, rules); return Promise.resolve(); }

      var productId = widget.getAttribute('data-product-id');
      return o.data.weightFor(productId, o.getVariantId()).then(function (kg) {
        if (mySeq !== seq) return;
        var flat = rules.flatRate(comuna.id, kg, cfg);
        var render = function (quotes) {
          if (mySeq !== seq) return;
          last = { comuna: comuna, kg: kg, flat: flat, quotes: quotes };
          renderPriced(comuna, flat, quotes);
        };
        if (flat != null) render([]); else ui.renderWidget(widget, { kind: 'loading', comuna: comuna }, rules);
        return o.data.quote(comuna, kg).then(render);
      }).catch(function (err) {
        if (typeof console !== 'undefined') console.warn('[eta] update failed', err);
        if (mySeq === seq) ui.renderWidget(widget, { kind: 'unavailable', comuna: comuna }, rules);
      });
    }

    // Timer tick: only the day promise depends on the clock, so re-render it from
    // the stored result instead of re-fetching the cart and quotes.
    function refreshPromise() {
      if (!last || rules.pickPrice(last.flat, last.quotes).source !== 'flat') return;
      renderPriced(last.comuna, last.flat, last.quotes);
    }

    doc.addEventListener('eta:comuna-changed', function () { update(); });
    doc.addEventListener('change', function (e) {
      var t = e.target && e.target.closest && e.target.closest('product-form, form[name="buy"]');
      if (t) o.win.setTimeout(update, 0);
    });
    var counter = doc.querySelector('.theme-cart-counter');
    if (counter && o.win.MutationObserver) {
      new o.win.MutationObserver(function () { update(); })
        .observe(counter, { attributes: true, childList: true, characterData: true, subtree: true });
    }
    if (o.win.setInterval && !o.noInterval) o.win.setInterval(refreshPromise, 60000);

    return { update: update, refreshPromise: refreshPromise };
  }

  function getCartPromise(win) {
    return new Promise(function (resolve) {
      if (!win.Jumpseller || !win.Jumpseller.getCart) { resolve(null); return; }
      setTimeout(function () { resolve(null); }, 3000); // getCart may never call back
      win.Jumpseller.getCart({ callback: function (c) { resolve(c && Array.isArray(c.products) ? c : null); } });
    });
  }

  function geolocate(win, comunas, rules, ui) {
    return new Promise(function (resolve) {
      if (!win.navigator.geolocation) { resolve(null); return; }
      win.navigator.geolocation.getCurrentPosition(function (pos) {
        var url = 'https://nominatim.openstreetmap.org/reverse?format=json&addressdetails=1&zoom=10' +
          '&lat=' + pos.coords.latitude + '&lon=' + pos.coords.longitude;
        win.fetch(url).then(function (r) { return r.json(); })
          .then(function (j) { resolve(ui.geoToComuna(j, comunas, rules)); })
          .catch(function () { resolve(null); });
      }, function () { resolve(null); }, { timeout: 10000 });
    });
  }

  function getVariantIdFrom(doc, from) {
    var pf = (from && from.closest && from.closest('product-form')) || doc.querySelector('product-form');
    if (pf && pf.variant && pf.variant.id != null) return String(pf.variant.id);
    var el = doc.querySelector('form[name="buy"] [name="variant_id"], form[name="buy"] [name="variant"]');
    return el && el.value ? el.value : null;
  }

  function autoStart(win) {
    var doc = win.document;
    var cfgEl = doc.getElementById('eta-config');
    if (!cfgEl || !win.Eta) return;
    var E = win.Eta;
    var storage = null;
    try { storage = win.localStorage; } catch (e) { storage = null; }
    var config = readConfig(cfgEl.dataset, E.Comunas, E.Rules);
    var app = start({
      doc: doc, win: win, config: config, comunas: E.Comunas, rules: E.Rules, ui: E.UI,
      store: E.Store.createStore({ storage: storage, eventTarget: doc }),
      data: E.Data.createData({
        rules: E.Rules, fetch: win.fetch.bind(win), storage: storage,
        getCart: function () { return getCartPromise(win); },
        now: function () { return Date.now(); },
        origin: config.origin, box: config.box, services: config.services,
      }),
      now: function () { return new Date(); },
      getVariantId: function () { return getVariantIdFrom(doc, doc.querySelector('[data-eta-widget]')); },
      geolocate: function () { return geolocate(win, E.Comunas, E.Rules, E.UI); },
    });
    app.update();
  }

  if (typeof window !== 'undefined' && typeof module === 'undefined') {
    if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', function () { autoStart(window); });
    else autoStart(window);
  }

  return { readConfig: readConfig, start: start, getVariantIdFrom: getVariantIdFrom };
});
